##REACT PROJECT 2

Installation

SERVER INSTALLATION
* `cd server`
* `npm install`
* `node server`


CLIENT INSTALLATION
* `npm client`
* `sudo npm install`
* `npm install material-ui`
* `sudo npm start`
* `npm install react-pure-grid --save`